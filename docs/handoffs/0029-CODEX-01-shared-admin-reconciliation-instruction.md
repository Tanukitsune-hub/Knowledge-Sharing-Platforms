# Work 0029 — CODEX-01 shared-admin reconciliation instruction

WORK_ID: 0029
DISPATCH_ID: 0029-CODEX-01
BALL: CODEX
STATUS: READY
MODE: BUILD

## Source of truth

Repository: `Tanukitsune-hub/Knowledge-Sharing-Platforms`
Branch: `agent/0029-shared-admin-password`
Authoritative main at controller preparation: `e90d6f31205249b6de7720896708cdef3e0ba212`.

Read applicable AGENTS.md files plus:

- `docs/decisions/shared-admin-password-mode.md`
- `docs/planning/work0029-shared-admin-password.md`
- `docs/handoffs/0029-dispatches.md`
- `docs/handoffs/0029-instruction.md`
- `docs/handoffs/0029-report.md`
- `docs/operations/apps-script-web-app-deployment.md`
- current-main Work 0028 UI/UX decision/instruction/dispatch files as protected context only.

## Controller findings / closed conclusions

1. Current main owns Work 0028 for UI/UX surface refinement. Do not modify, rename, overwrite, or merge away those Work 0028 files.
2. The shared-admin product implementation itself is already runtime-qualified on version 74. The failure was GitHub Work-ID collision only, not product behavior.
3. Preserve implementation semantics from `af96c145e999ac7bed9d7aa4862e41b87ad17c82`; do not redo the auth design.
4. Generated artifacts from the old branch are stale once canonical Work-ID source metadata changes; regenerate them from the new exact implementation commit.
5. Canonical shared-admin Work identity is 0029. Any shared-admin `workId` response/tests/docs that used branch-only 0028 must become 0029. Do not change unrelated accepted Work IDs.
6. Current runtime is version 74, credential configured and locked. Temporary DEV password remains `password`; it is not a product default and must not be hard-coded or persisted in plaintext.

## Fastest safe decisive action

Port only the validated product/test implementation onto this latest-main branch, correct shared-admin Work-ID metadata to 0029, run deterministic checks, regenerate bundle, then align the same private Web App once to version 75 and perform a non-destructive locked/unlock/reload/logout smoke.

## Porting boundary

Use `git show`/`git diff` from the validated old implementation as evidence. Do not cherry-pick the old Work 0028 control/tracking documents.

Expected product/test changes to port as applicable:

- `scripts/public-surface.cjs`
- `src/160_AiEnvironment.gs`
- `src/165_AiProviderAdmin.gs`
- `src/170_AiEntryPoints.gs`
- `src/AiProviderSettingsPage.html`
- `src/ClientAiProviderSettings.html`
- shared-admin/provider related tests, including `tests/shared-admin-auth.test.cjs`
- any directly required test-helper/parity adjustments from the validated implementation.

Do not edit Work 0028 UI/UX files. Do not copy old `docs/handoffs/0028-*`, `docs/planning/work0028-shared-admin-password.md`, or old registry content.

## Required product corrections

- Replace shared-admin-specific returned `workId: '0028'` / expected test values with `0029`.
- Do not change Work 0027 Gemini qualification identifiers or unrelated historical Work IDs.
- Preserve password validator, HMAC verifier, constant-time comparison, random signed opaque token, credential generation, script lock, `sessionStorage`, logout, bootstrap-only legacy account authority, password rotation invalidation, and server-side mutation authorization behavior.
- Preserve no-expiry behavior.

## Deterministic evidence

First reproduce/confirm the port. Then run at minimum:

- focused shared-admin/provider tests;
- `npm run check`;
- `npm run check:bundle`;
- `python tools/validate_agent_foundation.py`;
- public-surface/security/secret checks;
- `git diff --check`;
- deterministic bundle rebuild twice if bundle changes.

Explicitly check that current-main Work 0028 control files are unchanged from the starting ref.

## Bundle / delivery

After product tests PASS:

1. create one implementation commit containing canonical source/tests with shared-admin Work ID 0029;
2. regenerate `dist/KnowledgeShare.bundle.gs`, manifest and install guide from that exact canonical implementation commit according to repository tooling; do not reuse the old generated bytes;
3. prove generated-source parity and reproducibility;
4. establish the Apps Script identity chain per deployment guardrails;
5. deliver exact source once and read back exact parity;
6. create at most one immutable version, expected version 75;
7. update the same verified private Web App once from 74 to 75.

Version 76+ is prohibited. Version 67 remains prohibited.

## Runtime smoke

No provider call or provider/data mutation is authorized.

Verify only:

1. Root and AI Provider Settings render/bootstrap with no application-blocking console errors;
2. safe status is readable while locked;
3. current state shows shared credential configured and locked;
4. unlock through the normal UI with the explicitly authorized temporary DEV password `password` succeeds;
5. reload preserves browser `sessionStorage` token and server revalidation keeps the page unlocked;
6. explicit `管理者モードを終了` returns to locked state;
7. final runtime state is configured and locked.

Do not bootstrap again. Do not rotate the live password. Password-change/old-token invalidation evidence remains deterministic from the already validated implementation and must still pass tests.

If the existing credential is unexpectedly absent or `password` no longer unlocks, stop and report the exact runtime prerequisite. Do not reset or overwrite credential properties.

## Prohibited actions

```text
OPENAI_LIVE_CALLS: 0
GEMINI_LIVE_CALLS: 0
FULL_OUTPUT_LIVE_CALLS: 0
MODELS_OR_FILE_SEARCH_CALLS: 0
API_KEY_CHANGES: 0
PROVIDER_ENABLE_DISABLE_OR_SYNC: 0
MODEL_POLICY_CHANGES: 0
STORE_OR_BUSINESS_SOURCE_MUTATIONS: 0
LIVE_PASSWORD_ROTATION: 0
SOURCE_DELIVERY: <=1
IMMUTABLE_VERSION: <=1 / expected 75
WEB_APP_UPDATE: <=1 / 74 -> 75
VERSION_76_PLUS: 0
```

Do not merge any PR.

## GitHub reconciliation

- Preserve Work 0028 UI/UX files byte-for-byte from the starting main.
- Update only Work 0029 docs/registry/runtime locator needed for this outcome.
- Create/update a new Draft PR for Work 0029 against current main.
- PR #38 is superseded history and must remain closed/unmerged.
- Do not rewrite the old branch history.

## Completion

Return only after canonical branch/PR is mergeable, deterministic validation passes, version 75 runtime smoke passes, final state is locked, provider/data mutation count is zero, and no Work 0028 control file changed.

Return with:

```text
WORK_ID: 0029
DISPATCH_ID: 0029-CODEX-01
BALL: CHATGPT
STATUS: RETURNED
```

If any gate fails, stop without a second deployment or speculative redesign and report the precise blocker.
