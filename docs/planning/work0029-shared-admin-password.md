# Work 0029 — portable shared-password administrator mode

WORK_ID: 0029
MODE: BUILD
ACTIVE_DISPATCH_ID: 0029-CODEX-01
BALL: CODEX
STATUS: READY

## Primary outcome

Canonicalize and merge the already runtime-qualified shared administrator password feature on top of the latest `main`, without disturbing Work 0028 UI/UX design control.

Routine AI Provider Settings mutation authorization must be independent of Google account/email after bootstrap. The safe management page remains readable while locked. No time-based administrator-session expiry.

## Acceptance evidence hierarchy

1. GitHub identity: latest-main Work 0028 UI/UX files remain unchanged; shared-admin source is registered only as Work 0029 and PR is mergeable.
2. Product behavior: the validated shared-password auth primitive, UI, `sessionStorage` persistence, password rotation invalidation and server-side mutation token checks are preserved; shared-admin response `workId` values are 0029.
3. Deterministic validation: focused shared-admin tests, canonical `npm run check`, bundle, public-surface/security/secret checks and `git diff --check` pass.
4. Runtime alignment: exact source delivery/readback, same private Web App updated once from version 74 to 75, safe locked page PASS, unlock with the existing temporary DEV password PASS, reload/server revalidation PASS, explicit logout returns to locked state PASS.
5. Side effects: zero OpenAI/Gemini/FULL_OUTPUT/provider calls; zero API-key/model-policy/Store/business-source mutations; Work 0027 Gemini state remains qualified-disabled/hidden.

## Fastest safe decisive action

Start from latest main, port only the product/test implementation already proven in `af96c145e999ac7bed9d7aa4862e41b87ad17c82`, correct shared-admin Work metadata from 0028 to 0029, regenerate the bundle from the new exact implementation commit, run deterministic checks, then perform one bounded version-75 alignment/smoke verification.

Do not carry the superseded branch's `docs/handoffs/0028-*`, `docs/planning/work0028-*`, or registry edits into Work 0029.

## Required scope

- shared-password bootstrap/unlock/password-change/logout implementation already proven in version 74;
- opaque signed token, no account identity, no expiry, client `sessionStorage` only;
- server-side validation on every AI Provider Settings mutation;
- legacy account bootstrap only while shared credential is absent;
- safe read-only management when locked;
- update shared-admin response/report Work ID from 0028 to 0029;
- port tests and public-surface allowlist changes;
- regenerate distribution artifacts from the canonical implementation commit;
- one version-75 runtime alignment smoke.

## Non-goals

- Work 0028 UI/UX mocks or implementation;
- provider activation, qualification, API key change, sync, Store change, model-policy change;
- password rotation in live DEV during reconciliation;
- new auth architecture, MFA, timed expiry, lockout, Google Groups;
- company rollout, migration, large-file qualification.

## Authority and bounds

- Canonical branch: `agent/0029-shared-admin-password`.
- Starting main after controller recovery: latest `main`; preserve its Work 0028 files byte-for-byte unless unrelated main advancement requires a fresh controller decision.
- Preserved runtime evidence: version 74 qualified shared-admin mode, configured and locked, temporary DEV password still active.
- Port source/tests only from the validated implementation; regenerate `dist/` rather than reusing stale generated bytes.
- Source delivery/readback: max 1.
- Immutable version: max 1, expected 75.
- Same verified private Web App update: max 1, 74 -> 75.
- version 76+ prohibited in this dispatch; version 67 remains prohibited.
- provider/model/data live calls or mutations: 0.

## Closed conclusions

- Work 0028 belongs to UI/UX surface refinement and must remain unchanged.
- Work 0029 is the canonical identity for shared-password administration.
- The shared-admin product behavior itself already passed runtime qualification on version 74; the blocker was GitHub Work-ID collision, not a product defect.
- The temporary DEV bootstrap password remains `password` until the user changes it later through the implemented UI.
- Version 75 is authorized only to align the canonical Work 0029 source/runtime identity after the minimal Work-ID correction.

## Completion latch

Done when Work 0029 is mergeable from latest main, deterministic checks pass, version 75 serves the exact canonical source, the bounded locked/unlock/reload/logout smoke passes with no provider/data mutation, and no Work 0028 file/control is overwritten. Remaining password rotation by the user is follow-up, not a blocker.
